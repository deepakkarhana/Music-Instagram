# Normalise line endings so Windows and Linux checkouts produce identical
# diffs. Without this, every file looks "changed" when opened on another OS.
* text=auto eol=lf

# Never touch these
*.png binary
*.jpg binary
*.mp3 binary
*.npy binary
*.parquet binary
